node_modules/
dist/
build/
*.log
.env
.env.local
.DS_Store
coverage/
packages/*/dist/
packages/client/dist/
.vite/
*.tsbuildinfo
packages/server/vercel-out/
# Generated 3D assets (Meshy) ARE committed now, so they survive fresh clones /
# rebuilds instead of living only in the deployed dist (they used to vanish each
# session). ~400 KB each compressed.
